export default defineAppConfig({
    title: 'Forms | HRCG GmbH',
    theme: {
      dark: true,
      colors: {
        primary: '#ff0000'
      }
    },
    nuxtIcon: {
      size: '32px', // default <Icon> size applied
      class: 'icon', // default <Icon> class applied
      aliases: {
        'nuxt': 'logos:nuxt-icon',
      }
    }
  })